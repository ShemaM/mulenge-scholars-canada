# Join Flow Fix — Implementation Plan

## Steps
- [x] 1. Analyze codebase for loader/join usage patterns
- [x] 2. Gather findings on how users are told to join
- [x] 3. Create edit plan and get approval
- [x] 4. Add `successMessage` and `errorMessage` to `en.json` JoinPage namespace
- [x] 5. Add `successMessage` and `errorMessage` to `fr.json` JoinPage namespace
- [x] 6. Rewrite `JoinClient.tsx`: fix imports, URL normalization, redesign UI, HCI labels
- [ ] 7. Verify build and test

